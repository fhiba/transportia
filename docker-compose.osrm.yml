services:
  osrm:
    image: osrm/osrm-backend:latest
    container_name: transportia_osrm
    ports:
      - "5000:5000"
    volumes:
      - ./data/osrm:/data
    command: osrm-routed --algorithm mld /data/argentina-latest.osrm
    restart: unless-stopped
